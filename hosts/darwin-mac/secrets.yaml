# Secrets managed by agenix for darwin-mac
# Encrypt with: agenix -e secrets.yaml

# Example secrets structure (uncomment and customize):
# secret_name:
#   file = ./secrets/secret_name.age
#   owner = "xavtrav"
#   group = "staff"
#   mode = "0440"
